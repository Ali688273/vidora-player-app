<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- دسترسی خواندن ویدئوهای دستگاه -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <uses-permission
        android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- دسترسی اینترنت برای پخش ویدئوهای آنلاین -->
    <uses-permission
        android:name="android.permission.INTERNET" />

    <!-- سرویس پخش در پس‌زمینه -->
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE" />

    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/backup_rules"
        android:icon="@drawable/ic_vidora"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vidora"
        android:usesCleartextTraffic="true">

        <!--
            سرویس اصلی پخش.
            امکان ادامه پخش هنگام خاموش شدن صفحه
            و کنترل از اعلان/هدست/سیستم را فراهم می‌کند.
        -->
        <service
            android:name=".PlaybackService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">

            <intent-filter>

                <action
                    android:name="androidx.media3.session.MediaSessionService" />

                <action
                    android:name="android.media.browse.MediaBrowserService" />

            </intent-filter>

        </service>

        <!--
            پخش‌کننده اصلی.
            می‌تواند ویدئوهای محلی و لینک‌های مستقیم
            HTTP / HTTPS را دریافت کند.
        -->
        <activity
            android:name=".PlayerActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize"
            android:screenOrientation="unspecified"
            android:supportsPictureInPicture="true"
            android:exported="true">

            <!-- باز کردن فایل‌های ویدئویی از برنامه‌های دیگر -->
            <intent-filter>

                <action
                    android:name="android.intent.action.VIEW" />

                <category
                    android:name="android.intent.category.DEFAULT" />

                <category
                    android:name="android.intent.category.BROWSABLE" />

                <data
                    android:mimeType="video/*" />

            </intent-filter>

            <!-- دریافت لینک مستقیم HTTPS -->
            <intent-filter>

                <action
                    android:name="android.intent.action.VIEW" />

                <category
                    android:name="android.intent.category.DEFAULT" />

                <category
                    android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="https" />

            </intent-filter>

            <!-- دریافت لینک مستقیم HTTP -->
            <intent-filter>

                <action
                    android:name="android.intent.action.VIEW" />

                <category
                    android:name="android.intent.category.DEFAULT" />

                <category
                    android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="http" />

            </intent-filter>

        </activity>

        <!-- صفحه اصلی برنامه -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>

                <action
                    android:name="android.intent.action.MAIN" />

                <category
                    android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>

    </application>

</manifest>
